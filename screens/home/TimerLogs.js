import React from 'react';
import {SafeAreaView, StyleSheet, Text, View} from 'react-native';

const TimerLogs = () => {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>History of the sessions</Text>
    </SafeAreaView>
  );
};

export default TimerLogs;

const styles = StyleSheet.create({});